const FilterCountries = () => {
  return (
    <section id="filter">
      <input type="text" />
      <select>
        <option value="africa">africa</option>
      </select>
    </section>
  );
};

export default FilterCountries;
